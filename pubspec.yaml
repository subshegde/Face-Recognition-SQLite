name: facerecognition_flutter
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  facesdk_plugin:
    path: ./facesdk_plugin
  shared_preferences: ^2.0.13
  sqflite: ^2.0.0+3
  fluttertoast: ^8.0.9
  settings_ui: ^2.0.2
  image_picker: ^0.8.7
  flutter_facesdk: ^0.0.1
  flutter_exif_rotation: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
